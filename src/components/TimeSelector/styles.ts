import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    timeSelectorContainer: {
        display: 'flex',
        alignItems: 'center'
    },
    selectorContainer: {
        display: 'flex',
        flexDirection: 'row',
        height: 200,
    }
});
